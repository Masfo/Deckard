export module deckard.circular_buffer;

import std;
import deckard.types;
import deckard.assert;

namespace deckard
{
	template<typename T>
	class circular_buffer
	{
	private:
		std::vector<T> buffer;
	public:
	};

}
